// arraydiff.js

function elemInArray(elem, array) {
  return array.includes(elem);
}

export function arrayDiff(a, b) {
  if(Array.isArray(a))
  const union = new setInterval([...a], [...b]);
  return union;
}
